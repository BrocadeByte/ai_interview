# B2C 技术求职者产品优化任务计划

创建日期：2026-08-21

最后更新：2026-08-23，完成 FE-B2C-001、FE-B2C-002、FE-B2C-003、FE-B2C-004、FE-B2C-005 前端实现及 BE-B2C-001 简历模型与解析服务、BE-B2C-002 JD 模型与解析服务、BE-B2C-003 自动画像生成、BE-B2C-004 面试会话字段扩展、BE-B2C-005 面试规划按类型出题、BE-B2C-006 逐题复盘生成与保存、BE-B2C-007 专项练习与再测、BE-B2C-008 报告生命周期修正、BE-B2C-009 埋点与指标；前端移动端适配继承约束继续有效。

## 1. 文档目标

本文件用于把 AI 模拟面试系统的下一阶段方向，从“功能完整的通用模拟面试 MVP”收敛为“面向 B2C 技术求职者的岗位定制面试训练产品”，并拆成可以直接执行、验收和排期的研发任务。

本次优化只做文字面试和文字报告，不做语音、视频、TTS、ASR、口语流畅度分析，也不做企业招聘管理后台。语音能力从当前产品边界中移除，后续只有在文字训练闭环跑通、留存数据成立后再重新评估。

## 2. 当前项目判断

### 2.1 已具备的基础能力

- 已有注册登录、求职画像、模拟面试、流式出题、动态追问、逐题评分、报告生成、报告历史和知识库 RAG。
- 后端 `InterviewState` 已预留 `resume` 和 `target_job` 字段，适合承接简历解析和 JD 快照，不需要重做 LangGraph 状态图。
- `interview_scores` 已保存问题、原回答、扣分理由、短板、建议和知识引用，逐题复盘可以基于现有评分记录扩展。
- `interview_reports` 已保存总分、维度评分、优势、短板、建议、学习路线、示范回答和引用快照，适合继续增强为“报告短板到专项训练”的入口。
- 前端已有求职画像页、面试创建页、面试聊天页、报告详情页和报告列表页，下一步主要是重排首次体验和报告闭环。

### 2.2 当前最大缺口

- 品牌文案仍有“企业级面试训练平台”的表达，但现有流程明显更适合个人求职者。
- 首次体验依赖手动填写画像，用户需要自己组织技能、项目、目标岗位，启动成本偏高。
- 面试报告偏“总结型”，还没有把每道题拆成可训练、可复练、可对比的提升路径。
- 面试类型目前只有难度，没有训练/实战模式，也没有 HR、项目深挖、技术基础、系统设计等类型选择。
- 留存闭环尚未打通：用户看完报告后缺少“一键练短板、再测、看前后变化”的明确路径。

## 3. 产品定位与边界

### 3.1 第一阶段定位

产品名称继续使用：智面 AI。

一句话定位：

> 面向技术求职者的岗位定制 AI 面试训练助手。

核心承诺：

- 用简历和 JD 生成个人求职画像。
- 按目标岗位生成贴近真实面试的问题。
- 提供逐题复盘、扣分原因、建议结构和示范回答。
- 支持从报告短板一键进入专项练习，再通过再测看到提升。

### 3.2 目标用户

优先用户：

- 正在准备技术岗求职的应届生、实习生、社招初中级工程师。
- 岗位优先覆盖前端、后端 Java、后端 Python、AI 应用工程师、测试开发、算法工程师的通用训练框架。
- 用户有简历或项目经历，但不知道如何根据 JD 准备面试，也不知道自己回答哪里扣分。

暂不优先用户：

- 企业 HR 或招聘团队。
- 高阶架构师、技术管理者、总监级候选人。
- 非技术岗位，例如销售、运营、财务、法务。
- 需要语音、视频、真人面试官或线下服务的用户。

### 3.3 明确不做

- 不做语音面试、语音录制、ASR、TTS、视频面试和表情/眼神分析。
- 不做企业组织、候选人管理、招聘看板、多人协作、企业权限。
- 不做简历代投、岗位爬虫、招聘网站自动申请。
- 不做社区、课程平台、直播课和真人导师排班。
- 不在第一阶段做会员、支付、优惠券、分销等商业化系统。

### 3.4 品牌文案统一

需要替换或统一的文案：

| 场景 | 当前问题 | 建议文案 |
| --- | --- | --- |
| 顶部品牌副标题 | “企业级面试训练平台”偏 B2B | “技术求职者面试训练助手” |
| 登录页主标题 | 已偏求职者，可保留并强化 | “用简历和 JD，生成你的专属面试训练” |
| 注册页主标题 | “建立个人求职画像”方向正确 | “三分钟建立求职画像，开始岗位定制训练” |
| 面试首页标题 | “开始面试”偏泛 | “开始岗位定制训练” |
| 报告标题 | “面试复盘报告”可保留 | 增加“逐题复盘”和“专项练习入口” |
| 求职画像页 | 手动填写为主 | 增加“上传简历 / 粘贴 JD / 自动生成画像” |

## 4. 北极星体验

目标是把首次体验压缩到三分钟以内：

1. 用户注册或登录后进入“开始训练”。
2. 用户上传简历或粘贴简历文本。
3. 用户粘贴目标 JD，或只填写目标岗位。
4. 系统自动生成求职画像、岗位画像和面试建议。
5. 用户确认或轻量编辑。
6. 用户选择模式、面试类型、难度。
7. 立即开始文字面试。
8. 面试结束后查看逐题复盘报告。
9. 点击某个短板进入专项练习。
10. 完成专项后再测同类问题。
11. 报告展示前后对比：分数、短板数量、建议采纳情况、回答结构改善。

首轮 MVP 的关键体验不是“问完 8 题”，而是让用户明确感到：

> 我知道自己哪里差，也知道下一次怎么答，并且能马上练。

## 5. 信息架构调整

### 5.1 页面导航

建议第一阶段保留四个主入口：

- 开始训练：原“模拟面试”，承载简历/JD 输入、模式选择和面试启动。
- 训练报告：原“面试报告”，承载历史报告、逐题复盘和前后对比。
- 求职画像：承载简历版本、岗位目标、自动画像和手动补充。
- 知识库：继续作为管理员能力，普通求职者不突出展示。

如果当前没有角色权限区分，知识库可以先保留现状；面向普通用户的文案中不把知识库作为核心卖点。

### 5.2 首次体验入口

首次登录后建议进入“开始训练”，页面顶部直接提供三步：

| 步骤 | 用户动作 | 系统动作 | 目标耗时 |
| --- | --- | --- | --- |
| 1 | 上传简历或粘贴简历文本 | 提取教育、技能、项目、经历 | 30-60 秒 |
| 2 | 粘贴 JD 或填写目标岗位 | 提取岗位要求、技术栈、职责和加分项 | 30-60 秒 |
| 3 | 选择模式和面试类型 | 生成面试计划并启动首题 | 30-60 秒 |

允许跳过简历或 JD，但需要给出质量提示：

- 无简历：问题会更通用，项目深挖质量下降。
- 无 JD：系统按目标岗位通用能力模型出题。
- 两者都有：推荐标记为“岗位匹配训练”。

## 6. 核心业务闭环

### 6.1 闭环定义

核心闭环：

```text
完成面试
  -> 生成逐题复盘报告
  -> 识别短板
  -> 一键创建专项练习
  -> 针对短板追问训练
  -> 再测同类题
  -> 生成前后对比
  -> 推荐下一项短板
```

### 6.2 产品上必须可见的闭环入口

- 报告总览页展示“最需要提升的 3 项短板”。
- 每个短板旁边有“专项练习”按钮。
- 每道题复盘卡片有“重练这题”和“练同类题”按钮。
- 专项练习结束后必须进入“再测”，不能只做无限聊天。
- 再测完成后必须展示对比结果。

### 6.3 闭环成功标准

- 用户从报告点击专项练习后，系统能自动带入短板、原题、原回答、扣分原因和目标岗位。
- 专项练习的问题不重复原题文本，但考察同一个能力点。
- 再测评分与原评分可对比，至少展示总分变化、子分变化、短板是否缓解、回答结构是否更完整。
- 同一份报告可以产生多个专项练习，每个专项练习要有状态：未开始、训练中、待再测、已完成。

## 7. 数据模型设计

本节是建议的数据结构，第一阶段可以通过数据库字段和 JSON 字段增量落地，后续再根据查询频率拆表优化。

### 7.1 简历版本 `resumes`

用途：保存用户上传或粘贴的简历原文、解析结果和版本关系。

建议字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | int | 主键 |
| user_id | int | 所属用户 |
| source_type | string | `upload` / `paste` |
| file_name | string/null | 上传文件名 |
| file_type | string/null | `pdf` / `docx` / `txt` / `md` |
| raw_text | text | 提取后的简历文本 |
| parsed_json | text | LLM 结构化解析结果 |
| profile_patch_json | text | 可回填到 `user_profiles` 的字段 |
| status | string | `pending` / `parsed` / `failed` |
| error_message | text/null | 失败原因 |
| is_active | bool | 是否为默认简历 |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |

第一阶段建议支持：

- 粘贴简历文本。
- 上传 `.pdf`、`.txt`、`.md`。
- `.docx` 可作为 P1，如果要做，需要单独接入解析库和测试样本。

### 7.2 JD 快照 `job_descriptions`

用途：保存用户粘贴的 JD 和解析后的岗位画像，保证历史面试可复现。

建议字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | int | 主键 |
| user_id | int | 所属用户 |
| title | string | 岗位标题 |
| company_name | string/null | 公司名，可选 |
| raw_text | text | JD 原文 |
| parsed_json | text | 技术栈、职责、任职要求、加分项 |
| target_position | string | 归一化岗位 |
| is_active | bool | 是否为默认 JD |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |

解析结果建议结构：

```json
{
  "target_position": "前端开发工程师",
  "seniority": "初级/中级",
  "must_have_skills": ["Vue", "TypeScript", "工程化"],
  "nice_to_have_skills": ["性能优化", "Node.js"],
  "responsibilities": ["负责业务页面开发", "参与组件库建设"],
  "interview_focus": ["项目深挖", "技术基础", "工程实践"],
  "risk_points": ["缺少大型项目经验", "系统设计要求不高"]
}
```

### 7.3 用户画像扩展 `user_profiles`

现有字段可继续保留，新增或派生字段建议：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| active_resume_id | int/null | 当前默认简历 |
| active_job_description_id | int/null | 当前默认 JD |
| profile_source | string | `manual` / `resume` / `resume_jd` / `mixed` |
| profile_completeness | int | 画像完整度 0-100 |
| auto_summary | text/null | 自动画像摘要 |

也可以第一阶段不改表，先把这些信息保存在简历/JD 表中，接口返回时聚合。

### 7.4 面试会话扩展 `interview_sessions`

当前已有 `target_position`、`difficulty`、`status`、`interview_plan_json` 等字段。建议新增：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| mode | string | `training` / `mock`，训练/实战 |
| interview_type | string | `hr` / `project_deep_dive` / `technical_basics` / `system_design` / `mixed` |
| resume_id | int/null | 本场使用的简历版本 |
| job_description_id | int/null | 本场使用的 JD 快照 |
| parent_session_id | int/null | 再测或专项练习来源面试 |
| source_report_id | int/null | 从哪份报告发起 |
| source_weakness_key | string/null | 从哪个短板发起 |
| session_purpose | string | `full_interview` / `weakness_practice` / `retest` |
| comparison_group_id | string/null | 前后对比归组 |

### 7.5 逐题复盘 `question_reviews`

当前 `interview_scores` 已有大部分材料，但报告详情要稳定展示“问题、原回答、扣分原因、建议结构、示范回答”，建议新增逐题复盘表或在报告 JSON 中保存快照。

推荐新增表，避免报告重算后逐题内容漂移：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | int | 主键 |
| report_id | int | 所属报告 |
| session_id | int | 所属面试 |
| score_id | int | 来源评分 |
| question_index | int | 题号 |
| dimension | string | 考察维度 |
| question | text | 原问题 |
| answer | text | 原回答 |
| score | int | 本题得分 |
| sub_scores_json | text | 子分 |
| deduction_reasons_json | text | 扣分原因 |
| suggested_structure_json | text | 建议回答结构 |
| sample_answer | text | 示范回答 |
| practice_seed_json | text | 发起专项练习所需上下文 |
| created_at | datetime | 创建时间 |

`deduction_reasons_json` 可以第一阶段直接复用 `InterviewScore.reason` 和 `weaknesses`；`suggested_structure_json` 和 `sample_answer` 需要新增 LLM 生成或确定性模板补全。

### 7.6 专项练习 `practice_sessions`

专项练习可以复用 `interview_sessions`，也可以单独建表。为了减少改造，建议复用 `interview_sessions`，并通过 `session_purpose` 区分。

额外需要一张弱关系表保存短板来源：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | int | 主键 |
| user_id | int | 用户 |
| source_report_id | int | 来源报告 |
| source_session_id | int | 来源面试 |
| source_question_review_id | int/null | 来源题目 |
| weakness_key | string | 短板归一化 key |
| weakness_title | string | 短板标题 |
| target_dimension | string | 目标维度 |
| practice_session_id | int/null | 专项练习会话 |
| retest_session_id | int/null | 再测会话 |
| status | string | `not_started` / `practicing` / `ready_for_retest` / `completed` |
| before_score | int/null | 原始得分 |
| after_score | int/null | 再测得分 |
| comparison_json | text/null | 对比结论 |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |

## 8. 接口设计

### 8.1 简历接口

新增接口：

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| POST | `/resumes/upload` | 上传简历文件并解析 |
| POST | `/resumes/paste` | 粘贴简历文本并解析 |
| GET | `/resumes` | 简历版本列表 |
| GET | `/resumes/{id}` | 简历详情 |
| POST | `/resumes/{id}/activate` | 设为默认简历 |
| POST | `/resumes/{id}/apply-profile` | 将解析结果回填求职画像 |

`POST /resumes/paste` 请求：

```json
{
  "title": "前端开发简历 2026-08",
  "content": "简历正文..."
}
```

响应：

```json
{
  "id": 12,
  "status": "parsed",
  "profile_patch": {
    "education": "本科",
    "major": "计算机科学与技术",
    "experience_years": 2,
    "skills": "Vue, TypeScript, Vite, Element Plus",
    "projects": "AI 模拟面试系统...",
    "self_evaluation": "具备前后端协作和 AI 应用开发经验"
  },
  "parsed": {
    "projects": [],
    "skills": [],
    "experience_summary": ""
  }
}
```

验收要求：

- 文件过大、空文本、乱码、不可解析 PDF 要返回明确错误。
- 解析失败不能覆盖已有画像。
- 用户只能读取和激活自己的简历。
- 原文和解析结果要作为历史快照保存。

### 8.2 JD 接口

新增接口：

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| POST | `/job-descriptions/parse` | 粘贴 JD 并解析 |
| GET | `/job-descriptions` | JD 历史列表 |
| GET | `/job-descriptions/{id}` | JD 详情 |
| POST | `/job-descriptions/{id}/activate` | 设为默认 JD |

`POST /job-descriptions/parse` 请求：

```json
{
  "raw_text": "岗位 JD...",
  "title": "前端开发工程师",
  "company_name": "可选"
}
```

验收要求：

- 能从 JD 中抽取目标岗位、技术栈、职责、硬性要求、加分项和建议面试类型。
- JD 原文作为不可信输入进入 Prompt，必须继续使用现有 `format_untrusted_data` 与安全系统提示。
- JD 快照必须绑定到面试会话，历史报告读取时不能被后续 JD 修改影响。

### 8.3 自动画像接口

新增接口：

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| POST | `/profile/auto-generate` | 根据简历和 JD 生成画像草稿 |
| PUT | `/profile/me` | 继续复用现有画像保存接口 |

`POST /profile/auto-generate` 请求：

```json
{
  "resume_id": 12,
  "job_description_id": 5,
  "target_position": "前端开发工程师"
}
```

响应：

```json
{
  "profile_patch": {
    "target_position": "前端开发工程师",
    "skills": "Vue, TypeScript, Vite, 前端工程化",
    "projects": "项目 1...\n项目 2...",
    "self_evaluation": "适合偏 AI 应用与前端工程化岗位"
  },
  "completeness": 86,
  "warnings": [
    "简历中缺少明确量化结果，项目深挖可能需要补充指标"
  ]
}
```

验收要求：

- 自动画像只生成草稿，必须用户确认后才覆盖 `user_profiles`。
- 页面要高亮“来自简历/JD 的字段”和“用户手动修改的字段”。
- 画像字段必须设置最大长度，避免超长简历直接写入画像表。

### 8.4 面试创建接口扩展

现有 `POST /interviews` 只接收 `target_position` 和 `difficulty`。建议扩展为：

```json
{
  "target_position": "前端开发工程师",
  "difficulty": "medium",
  "mode": "training",
  "interview_type": "project_deep_dive",
  "resume_id": 12,
  "job_description_id": 5,
  "source_report_id": null,
  "source_weakness_key": null,
  "session_purpose": "full_interview"
}
```

字段约束：

- `mode`: `training` 或 `mock`。
- `interview_type`: `hr`、`project_deep_dive`、`technical_basics`、`system_design`、`mixed`。
- `session_purpose`: `full_interview`、`weakness_practice`、`retest`。
- 训练模式默认允许实时评分和提示；实战模式过程中隐藏评分，只在结束后展示报告。

兼容策略：

- 前端旧请求不传新字段时，后端默认 `mode=training`、`interview_type=mixed`、`session_purpose=full_interview`。
- 旧历史会话读取时缺失字段按默认值展示。

### 8.5 逐题复盘接口

新增接口：

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| GET | `/reports/{report_id}/question-reviews` | 获取逐题复盘 |
| GET | `/interviews/{session_id}/question-reviews` | 按会话获取逐题复盘 |

复盘项响应：

```json
{
  "question_index": 1,
  "dimension": "项目经验",
  "question": "请介绍一个你解决复杂问题的项目...",
  "answer": "我的回答...",
  "score": 68,
  "sub_scores": {
    "专业准确性": 70,
    "表达清晰度": 65,
    "岗位匹配度": 70
  },
  "deduction_reasons": [
    "回答缺少量化结果",
    "技术方案没有说明取舍"
  ],
  "suggested_structure": [
    "背景：说明业务目标和约束",
    "任务：明确自己的职责",
    "行动：解释方案、取舍和实现细节",
    "结果：给出指标和复盘"
  ],
  "sample_answer": "可以这样回答：..."
}
```

验收要求：

- 每条复盘必须能追溯到 `interview_scores`。
- 每题至少包含问题、原回答、扣分原因、建议结构和示范回答。
- 示范回答不能编造候选人没有提供的经历；如果信息不足，要使用“可补充信息占位”提示用户补充。

### 8.6 专项练习和再测接口

新增接口：

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| POST | `/practice/from-report` | 从报告短板创建专项练习 |
| POST | `/practice/from-question-review` | 从某道题创建专项练习 |
| GET | `/practice` | 专项练习列表 |
| POST | `/practice/{id}/start` | 启动专项练习会话 |
| POST | `/practice/{id}/start-retest` | 启动再测会话 |
| GET | `/practice/{id}/comparison` | 获取前后对比 |

创建专项练习请求：

```json
{
  "report_id": 23,
  "question_review_id": 101,
  "weakness_key": "project_result_metrics"
}
```

对比响应：

```json
{
  "before": {
    "session_id": 51,
    "score": 68,
    "weaknesses": ["缺少量化结果", "方案取舍不清晰"]
  },
  "after": {
    "session_id": 58,
    "score": 82,
    "weaknesses": ["方案取舍仍可更具体"]
  },
  "delta": {
    "score": 14,
    "resolved_weaknesses": ["缺少量化结果"],
    "remaining_weaknesses": ["方案取舍仍可更具体"]
  },
  "summary": "回答已经补充指标，但技术取舍还可以更明确。"
}
```

## 9. Agent 与 Prompt 改造

### 9.1 面试规划节点

输入需要从当前“画像 + 岗位”扩展为：

- 用户画像。
- 简历解析摘要。
- JD 解析摘要。
- 模式：训练 / 实战。
- 面试类型：HR / 项目深挖 / 技术基础 / 系统设计 / 综合。
- 是否为专项练习或再测。
- 来源短板、原题、原回答、扣分原因。

规划要求：

- 全量面试仍默认 8 个主问题。
- 专项练习建议 3-5 个问题。
- 再测建议 1-2 个高相似能力点问题。
- 项目深挖优先围绕简历项目，不要泛泛问八股。
- 技术基础优先覆盖 JD 中 must-have 技能。
- 系统设计按岗位年限控制难度，初级岗位不要过度架构化。
- HR 类型关注动机、稳定性、沟通协作、冲突处理和职业规划。

### 9.2 回答评估节点

新增评分上下文：

- 当前模式。
- 当前面试类型。
- 来源短板。
- JD 要求。
- 简历项目事实。

训练模式：

- 每题后允许给出即时得分、扣分原因和一句改进提示。
- 可以更教学化，指出回答结构问题。

实战模式：

- 面试过程中不返回实时评分提示。
- 后端仍保存评分，用于最终报告。
- 前端隐藏 LiveScorePanel，结束后展示完整报告。

### 9.3 报告生成节点

报告要从“总体总结”升级为“总体结论 + 逐题复盘 + 练习入口”。

必须输出：

- 总分和维度分。
- 最关键的 3 个短板。
- 每题复盘：
  - 问题。
  - 原回答。
  - 扣分原因。
  - 建议结构。
  - 示范回答。
  - 关联短板 key。
- 推荐练习顺序。
- 再测建议。

示范回答约束：

- 基于用户已提供的简历、回答和画像。
- 不得虚构公司、岗位、指标、技术栈。
- 如果必须举例，要明确标注“可替换为你的真实数据”。

## 10. 前端任务拆解

### 10.1 前端移动端继承约束

约束来源：`前端移动端适配任务跟踪.md`。

该文档中的 FE-MOB-001～FE-MOB-112 已完成，后续 B2C 优化不得把移动端适配当成空白重新设计，也不得破坏已验收的手机端布局、软键盘、底部导航、安全区和构建治理成果。新功能要在既有移动端体系上增量扩展。

必须继承的规则：

- 保留 `frontend/index.html` 中已补齐的 viewport、`viewport-fit=cover`、页面语言、主题色和描述设置。
- 沿用 `1023px / 900px / 767px / 480px` 四档响应式规则，不为单个新页面随意新增冲突断点。
- 手机端继续使用已验收的底部导航和安全区方案，页面底部内容必须避开 `--mobile-nav-height` 与 `safe-area-inset-bottom`。
- 新增上传简历、粘贴 JD、自动画像、逐题复盘、专项练习和前后对比页面时，必须在 390×844 手机视口下无横向溢出、无内容遮挡、无按钮文字截断。
- 面试聊天页不能破坏已完成的手机聊天布局；回答框仍需适配动态高度、软键盘收缩和长文本换行。
- 实时评分在移动端仍按 FE-MOB-006 的折叠/覆盖面板思路处理；新增训练/实战模式时，不能让评分面板挤压聊天区。
- 知识库移动端两级布局、未保存提醒、触控区和普通用户权限隐藏规则保持不变。
- 样式继续沿用 FE-MOB-109 的拆分结构，新增样式优先放入对应页面样式文件或响应式文件，不把全局 `styles.css` 重新堆大。
- Element Plus 组件继续按需导入，遵守 FE-MOB-112 的包体积治理结果，不恢复大规模全局注册。
- `frontend/src` 不应重新出现构建生成的 `.js` 或 `.vue.js` 副本。

移动端验收基线：

- 浏览器尺寸至少覆盖 320×568、390×844、430×932、768×1024、1024×900、1440×900。
- 面试相关页面额外覆盖手机横屏 844×390，以及输入框聚焦后可视区缩小场景。
- 关键页面要复用或更新 `output/playwright/responsive-baselines/` 下的响应式截图基线。
- 每次涉及前端布局的新任务完成后，必须执行 `npm run typecheck` 和 `npm run build`；涉及页面结构变化时补 Playwright 响应式回归。
- FE-MOB-113 物理 iOS/Android 真机抽验仍是后续补充项；自动化视口验收通过不等于物理真机完全通过。

### FE-B2C-001 品牌文案统一

优先级：P0

状态：已完成（2026-08-22）

涉及文件：

- `frontend/src/components/layout/AppShell.vue`
- `frontend/src/views/LoginView.vue`
- `frontend/src/views/RegisterView.vue`
- `frontend/src/views/InterviewHomeView.vue`
- `frontend/index.html`

任务：

- 将“企业级面试训练平台”替换为 B2C 求职者定位文案。
- 页面标题和描述统一为“智面 AI / 技术求职者面试训练助手”。
- 避免对外展示企业招聘、企业级、候选人管理等未实现能力。

验收：

- 全局搜索不再出现误导性 B2B 主文案。
- 登录、注册、开始训练、报告页的价值表达一致。

完成记录：

- 全局品牌统一为“智面 AI / 技术求职者面试训练助手”，移除“企业级面试训练平台”等 B2B 定位文案。
- 导航、登录、注册、开始训练和报告页统一为技术求职者的岗位定制训练表达。
- 未改动页面结构、响应式断点、底部导航、安全区或 Element Plus 按需引入策略。
- 验证：`npm run typecheck` 和 `npm run build` 均通过；全局文案审计未发现“企业级”“企业招聘”“候选人管理”等误导性 B2B 表达；`frontend/src` 下未新增构建生成的 `.js` 或 `.vue.js` 副本。

### FE-B2C-002 开始训练页增加三分钟入口

优先级：P0

状态：已完成（2026-08-22；后端依赖接口完成后补真实联调）

涉及文件：

- `frontend/src/views/InterviewHomeView.vue`
- `frontend/src/api/interview.ts`
- `frontend/src/api/profile.ts`
- 新增 `frontend/src/api/resume.ts`
- 新增 `frontend/src/api/jobDescription.ts`

任务：

- 在面试设置前增加“简历 / JD / 自动画像”区域。
- 支持粘贴简历文本。
- 支持上传简历文件。
- 支持粘贴 JD。
- 展示自动画像摘要和完整度。
- 用户确认画像后再创建面试。

交互细节：

- 没有简历时仍可开始，但显示“通用训练”提示。
- 有简历无 JD 时显示“简历定制训练”。
- 有简历和 JD 时显示“岗位匹配训练”。
- 解析中按钮进入 loading，并禁止重复提交。
- 解析失败保留用户输入，不清空表单。

验收：

- 新用户可以在一个页面内完成简历/JD 输入、画像生成和面试启动。
- 核心路径不超过 3 分钟。
- 移动端 390px 宽度下表单不横向溢出。

完成记录：

- 开始训练页在训练设置前新增简历/JD/自动画像三分钟入口，支持粘贴简历、上传 PDF/TXT/MD 简历、粘贴 JD，并限制文件类型、10MB 文件大小和输入最大长度。
- 新增 `resume.ts`、`jobDescription.ts` 接口封装和自动画像前端契约；面试创建请求会携带已解析的 `resume_id` 与 `job_description_id`。
- 根据材料实时展示“通用训练 / 简历定制训练 / JD 定制训练 / 岗位匹配训练”；未提供简历/JD 时保留通用训练路径。
- 解析、画像生成和画像确认分别提供 loading 与重复提交保护；失败时保留简历文件、简历文本和 JD 文本，已成功解析的阶段可直接复用重试。
- 自动画像展示完整度、摘要和风险提示；提供材料时必须确认并保存画像后才解锁训练，材料变化会使旧画像确认失效。
- 验证：`npm run typecheck`、`npm run build` 均通过；使用固定模拟接口完成画像生成与确认交互回归；320×568、390×844、844×390、1440×900 均无横向溢出，手机底部导航和安全区规则未被破坏。
- 依赖说明：BE-B2C-001～BE-B2C-003 的简历、JD 和自动画像接口已经落地；BE-B2C-004 的简历/模式/类型会话字段仍待实现，首次训练完整链路仍需在该任务完成后补真实接口联调。

### FE-B2C-003 模式与面试类型选择

优先级：P0

状态：已完成（2026-08-22）

涉及文件：

- `frontend/src/views/InterviewHomeView.vue`
- `frontend/src/api/interview.ts`
- `frontend/src/views/InterviewChatView.vue`
- `frontend/src/components/interview/LiveScorePanel.vue`

任务：

- 增加训练/实战模式切换。
- 增加面试类型选择：综合、HR、项目深挖、技术基础、系统设计。
- 在面试会话顶部展示当前模式和类型。
- 实战模式隐藏实时评分与即时提示。
- 训练模式保留实时评分。

验收：

- 创建会话时请求体包含 `mode` 和 `interview_type`。
- 实战模式中用户答题过程看不到实时得分。
- 训练模式中保持现有实时评分体验。

完成记录：

- 开始训练页新增训练/实战模式切换，以及综合、HR、项目深挖、技术基础、系统设计五类面试选择；模式说明和开始按钮会随选择同步变化。
- `InterviewCreateInput` 和 `InterviewSession` 已补齐 `mode`、`interview_type` 类型契约，创建请求会提交两个字段；旧会话缺字段时按 `training`、`mixed` 展示。
- 会话顶部展示当前模式和面试类型；实战模式不请求评分接口、不渲染桌面或移动端实时评分面板，并隐藏评分中的过程提示；训练模式继续加载和展示实时得分、评分理由与改进建议。
- 在后端会话字段尚未落地期间，前端会按会话 ID 在浏览器本地保留刚创建会话的模式与类型，避免接口响应暂缺字段时把实战会话误判为训练会话。
- 响应式回归覆盖 320×568、390×844、430×932、768×1024、1024×900、1440×900，以及面试页 844×390 横屏；各视口无横向溢出，320px 下五类选项文字完整可读，实战页评分面板数量为 0。
- 验证：`npm run typecheck`、`npm run build` 均通过；Playwright 固定模拟数据确认创建请求体为 `mode=mock`、`interview_type=system_design`，实战会话未发起评分请求，训练会话仍请求评分接口并展示实时评分。
- 依赖说明：当前仓库尚未实现 BE-B2C-004、BE-B2C-005，后端仍需持久化模式/类型并按类型出题；前端本地兼容仅用于避免联调前误展示评分，不能替代后端字段与 Agent 逻辑。

### FE-B2C-004 报告详情改成逐题复盘

优先级：P0

状态：已完成（2026-08-22；后端依赖接口完成后补真实联调）

涉及文件：

- `frontend/src/views/ReportDetailView.vue`
- `frontend/src/api/report.ts`
- `frontend/src/styles/pages/reports.css`

任务：

- 在总体评价和维度评分之后新增“逐题复盘”。
- 每题以可折叠卡片展示问题、原回答、扣分原因、建议结构、示范回答。
- 每题提供“重练这题”和“练同类题”按钮。
- 短板区域增加“专项练习”按钮。

验收：

- 报告至少可以展示 8 道主问题的复盘。
- 长回答、长示范回答在桌面和移动端都可读，不撑破布局。
- 点击练习按钮能带着题号和短板创建专项练习。

完成记录：

- 报告在总体评价和维度评分后新增“逐题复盘”，按主问题排序展示可折叠卡片，包含题目、原回答、扣分原因、建议结构和逐题示范回答。
- 新增报告/会话两种逐题复盘读取契约；在 BE-B2C-006 尚未落地时自动降级为现有逐题评分记录，不会把报告级示例冒充为某一题的示范回答。
- 每题新增“重练这题”和“练同类题”，待提升项新增“专项练习”；请求会带入报告 ID、复盘/评分 ID、题号、短板 key、短板文案和练习类型，后端返回练习会话时直接进入会话。
- 长回答和长示范回答使用可换行的独立内容区，卡片、双列复盘内容和操作按钮在窄视口自适应收缩/换行。
- 验证：`npm run typecheck`、`npm run build` 均通过；Playwright 固定模拟数据展示 8 道主问题，完成展开收起、长文本、练习请求参数和接口成功态回归；320×568、390×844、844×390、1440×900 均无横向溢出，控制台无错误，已补充报告详情桌面/手机截图基线。
- 依赖说明：BE-B2C-006 已按第 8.5 节契约完成逐题复盘持久化及报告/会话读取接口，前端可直接读取真实复盘；BE-B2C-007 的专项练习接口仍待实现，现有旧评分降级继续作为异常兜底。

### FE-B2C-005 前后对比视图

优先级：P1

状态：已完成（2026-08-22；后端依赖接口完成后补真实联调）

涉及文件：

- 新增 `frontend/src/views/PracticeComparisonView.vue`
- `frontend/src/router/index.ts`
- `frontend/src/api/practice.ts`
- `frontend/src/styles/pages/reports.css`

任务：

- 展示原始得分、再测得分和分数变化。
- 展示已缓解短板、仍需改进短板。
- 展示原回答和再测回答的结构对比。
- 提供“继续练下一个短板”入口。

验收：

- 完成再测后自动跳转到对比页。
- 对比页能准确展示 before/after session。
- 没有再测结果时显示“待再测”，而不是空白页。

完成记录：

- 新增 `/practice/:id/comparison` 对比路由和 `PracticeComparisonView.vue`，展示原始/再测会话编号、总分与子分变化、已缓解/仍需改进短板、两版回答正文和结构化步骤对照。
- `practice.ts` 补齐练习列表、开始专项、开始再测、读取对比结果的前端契约及数据归一化；兼容尚无 `after` 的待再测结果，并对缺失数组、子分和文本提供确定性空状态。
- 再测会话完成时会根据 `session_purpose=retest` 与后端返回的练习 ID 自动跳转对比页；同时支持 `?practiceId=` 作为后端字段落地前后的导航兼容参数。
- 对比完成页提供“继续练下一个短板”入口：后端返回下一短板时直接创建专项练习，否则回到来源报告的待提升项。
- 验证：`npm run typecheck`、`npm run build` 均通过；Playwright 固定模拟数据确认 before 会话 #51、after 会话 #58、总分变化 +14 和再测自动跳转；`after: null` 明确展示“待再测”。
- 响应式回归覆盖 1440×900、390×844、320×568、844×390，各视口无横向溢出、按钮文字无截断；已补充对比页桌面/手机截图基线。
- 依赖说明：当前仓库尚未实现 BE-B2C-007 的专项练习、再测和对比接口；本任务已按第 8.6 节完成前端和固定模拟接口回归，待后端任务落地后补真实接口联调。

## 11. 后端任务拆解

### BE-B2C-001 简历模型与解析服务

优先级：P0

状态：已完成（2026-08-22）

任务：

- 新增 `Resume` ORM 模型。
- 新增简历 Pydantic Schema。
- 新增粘贴简历接口。
- 复用知识库已有 PDF/text 解析能力，但简历内容不能进入管理员知识库分类。
- 调用 LLM 生成结构化简历解析结果。
- 生成可回填 `user_profiles` 的 `profile_patch`。

验收：

- 支持粘贴简历生成画像草稿。
- 支持 PDF/txt/md 上传解析。
- 解析失败不会污染用户画像。
- 单元测试覆盖空文本、超长文本、解析 JSON 异常、权限隔离。

完成记录：

- 新增 `Resume` ORM 模型，保存用户、标题、来源类型、文件信息、简历原文、结构化解析结果、画像草稿、解析状态、错误信息、激活状态和版本时间；简历表独立于管理员知识库，不创建知识库文档或向量数据。
- 新增简历 Pydantic Schema 和严格 LLM 输出结构，画像字段设置长度/数值边界；粘贴文本限制 20,000 字符，上传文件限制 PDF/TXT/MD、10MB，并复用知识库已有文件解析器。
- 新增 `/api/resumes/paste`、`/upload`、列表、详情、激活和显式回填画像接口；解析阶段只保存画像草稿，只有用户调用 `apply-profile` 才更新 `user_profiles`，解析失败会保存 `failed` 状态且不污染现有画像。
- 简历原文通过 `format_untrusted_data` 进入安全 Prompt，严格校验并单次修复 LLM JSON；用户只能读取、激活和回填自己的简历，跨用户访问统一返回 404。
- 测试覆盖空文本、超长文本、TXT/MD 上传、PDF 解析器复用、LLM 非法 JSON、解析失败画像不变、权限隔离和 Prompt injection 边界；`python -m compileall -q app test` 通过，`python -m pytest test -q` 全量 130 项通过。

### BE-B2C-002 JD 模型与解析服务

优先级：P0

状态：已完成（2026-08-23）

任务：

- 新增 `JobDescription` ORM 模型。
- 新增 JD 解析接口。
- 抽取岗位、年限、技术栈、职责、加分项、面试重点。
- 将 JD 作为本场面试快照绑定到 `interview_sessions`。

验收：

- JD 修改后不影响历史面试报告。
- 面试规划 Prompt 可以读取本场绑定的 JD 快照。
- 测试覆盖无 JD、普通 JD、超长 JD 和 prompt injection 文本。

完成记录：

- 新增 `JobDescription` ORM 模型和严格 Pydantic Schema，保存所属用户、标题、公司、JD 原文、结构化解析结果、归一化目标岗位、解析状态、激活状态和版本时间；JD 原文限制 20,000 字符。
- 新增 `/api/job-descriptions/parse`、列表、详情和激活接口；结构化结果覆盖岗位、职级/年限、硬性技能、加分技能、职责、硬性要求、面试重点和风险点，跨用户读取、激活或绑定统一返回 404。
- JD 通过 `format_untrusted_data` 和安全系统提示进入 LLM，严格校验 JSON Schema 并允许一次 JSON 修复；prompt injection 内容始终位于 `UNTRUSTED` 数据边界，不能改变系统角色和输出结构。
- 面试创建接口新增可选 `job_description_id`，只允许绑定当前用户已解析的 JD；创建时把 JD 原文和解析结果序列化到 `interview_sessions.job_description_snapshot_json`，后续 JD 变化不会改变历史会话使用的快照。
- `build_state_from_session` 将冻结快照注入 `target_job`，`interview_planner` 的系统提示和用户提示会读取本场 JD 的硬性技能、职责和面试重点；未传 JD 时保持原有通用岗位状态和旧请求兼容。
- 测试覆盖普通 JD、空文本、超长文本、无 JD 创建、权限隔离、快照不可变和 prompt injection/规划 Prompt 安全边界；`python -m compileall -q app test` 通过，`python -m pytest test -q` 全量 135 项通过。

### BE-B2C-003 自动画像生成

优先级：P0

状态：已完成（2026-08-23）

任务：

- 新增 `POST /profile/auto-generate`。
- 合并简历解析结果、JD 解析结果和已有手动画像。
- 输出画像草稿、完整度、风险提示。
- 用户确认后复用 `PUT /profile/me` 保存。

验收：

- 自动画像不直接覆盖用户已有画像。
- 用户可以选择应用全部字段或部分字段。
- 画像完整度低于阈值时，前端提示还需补充哪些信息。

完成记录：

- 新增 `POST /api/profile/auto-generate`，按当前用户权限读取已解析简历与 JD，并与已有画像合并；跨用户资源返回 404，未解析或损坏的结构化快照返回 409。
- 合并规则保持候选人事实与岗位要求分离：已有画像用于补缺，简历画像草稿更新候选人事实，JD 和请求参数只用于确定目标岗位及生成匹配风险，不会把 JD 硬性技能直接写成用户已掌握技能。
- 接口只返回可确认的 `profile_patch`，不创建或更新 `user_profiles`；同时返回 0～100 完整度、自动摘要、风险提示、缺失字段和字段来源，低于 70% 时明确列出仍需补充的信息。
- `PUT /api/profile/me` 改为按请求中实际提供的字段更新，支持应用全部字段或只应用用户选择的部分字段；显式传 `null` 仍可清空单个字段，未选择字段保持不变。
- 画像写入 Schema 补齐字符串最大长度，避免超长简历内容绕过草稿边界直接写入画像；JD 未在简历/现有画像中体现的硬性技能只作为“请按真实经历确认”的风险提示。
- 测试覆盖草稿不落库、多源优先级、部分字段应用、低完整度缺失提示、JD 技能不冒充候选人技能、权限隔离、未解析/损坏快照和超长字段；`python -m compileall -q app test` 通过，`python -m pytest test -q` 全量 141 项通过。

### BE-B2C-004 面试会话字段扩展

优先级：P0

状态：已完成（2026-08-23）

任务：

- 扩展 `InterviewSession` 模型和迁移脚本。
- 扩展 `InterviewCreate` Schema。
- `build_state_from_session` 注入简历、JD、模式、类型、练习来源。
- 旧会话兼容默认值。

验收：

- 旧测试保持通过。
- 创建训练模式、实战模式、不同面试类型时状态图收到正确字段。
- 旧历史会话可以正常打开和生成报告。

完成记录：

- `InterviewSession` 新增 `mode`、`interview_type`、`resume_id`、`parent_session_id`、`source_report_id`、`source_weakness_key`、`session_purpose` 和 `comparison_group_id`；沿用仓库启动期增量迁移机制补齐列与索引，非空枚举字段使用兼容旧数据的数据库默认值。
- 面试创建 Schema 支持训练/实战模式、五类面试类型、简历/JD、练习来源和对比分组；旧请求默认 `mode=training`、`interview_type=mixed`、`session_purpose=full_interview`，详情及列表接口均返回新字段。
- 创建会话时校验简历、JD、父会话和来源报告均属于当前用户，且简历/JD 必须已解析；新增 `resume_snapshot_json` 并继续使用 JD 快照，保证后续源数据变化不会影响本场面试和历史报告上下文。
- `build_state_from_session` 将简历快照、JD 快照、模式、类型、资源 ID、会话用途、父会话、来源报告、短板 key 和对比分组注入 LangGraph 状态；缺失新字段的旧会话确定性降级为训练、综合、完整面试且无简历/JD。
- 测试覆盖完整字段创建、训练/实战与不同类型校验、快照不可变、状态注入、旧请求与旧状态默认值、列表读取和跨用户来源隔离；`python -m compileall -q app test` 通过，`python -m pytest test -q` 全量 144 项通过。

### BE-B2C-005 面试规划按类型出题

优先级：P0

状态：已完成（2026-08-23）

任务：

- 修改 `interview_planner.py` 的系统提示和用户提示。
- 根据 `interview_type` 调整维度和题量。
- 根据 `mode` 控制题目反馈策略。
- 专项练习和再测使用更短的问题计划。

验收：

- HR 类型不生成纯技术八股题。
- 项目深挖优先引用简历项目。
- 技术基础优先覆盖 JD must-have 技能。
- 系统设计题不会超出岗位年限太多。

完成记录：

- 面试规划系统提示和用户提示已注入模式、五类面试类型、简历快照、JD 快照、会话用途、来源会话/报告/短板及知识库上下文，并继续用不可信数据边界隔离候选人材料和检索内容。
- 为 HR、项目深挖、技术基础、系统设计和综合提供差异化维度与确定性兜底首题/计划：HR 限定非纯技术能力，项目深挖优先简历真实项目，技术基础优先 `parsed.must_have_skills`，系统设计按候选人和 JD 年限限制规模与复杂度。
- 完整面试保持 8 个主问题，专项练习固定为 4 个渐进问题，再测固定为 2 个同能力点不同题面的问题；计划归一化会校准题量与权重，状态图、回答链路和接口总题数改为读取持久化计划，短计划可按实际题量自动结束，旧空计划仍兼容 8 题。
- 训练模式允许诊断性、渐进式追问并保留即时反馈体验；实战模式要求问题保持中性，不在题面泄露得分、答案、短板或改进提示，后台评分仍完整保存供最终报告使用。
- 测试覆盖类型兜底维度、三种会话用途题量、模式/类型/简历/JD/来源上下文注入、类型化异常兜底、短计划自动收尾、接口动态总题数和旧计划兼容；`python -m compileall -q app test` 通过，`python -m pytest test -q` 全量 153 项通过。

### BE-B2C-006 逐题复盘生成与保存

优先级：P0

状态：已完成（2026-08-23）

任务：

- 新增 `QuestionReview` 模型或报告快照字段。
- 基于 `InterviewScore` 生成逐题复盘。
- 为每题生成建议结构和示范回答。
- 将复盘结果绑定报告保存。
- 新增读取接口。

验收：

- 每条评分都有对应复盘。
- 复盘缺字段时走确定性兜底，不保存空复盘。
- 示范回答遵守不编造事实约束。

完成记录：

- 新增独立 `QuestionReview` ORM 模型，以 `report_id + score_id` 唯一绑定报告和来源评分，保存题号、维度、原问题、原回答、总分/子分、扣分原因、短板、建议结构、示范回答、短板 key 和专项练习上下文快照；新表由现有启动期 `Base.metadata.create_all` 机制创建。
- 报告首次生成及后续读取时都会按评分记录补齐缺失复盘；同一报告已有的完整复盘保持不变，来源评分后续变化不会导致历史复盘漂移，损坏或缺字段的复盘会从来源评分走确定性兜底修复，不保存空问题、空回答、空扣分原因、空建议结构或空示范回答。
- 建议结构按项目/行为、系统设计、技术基础等维度使用可执行模板，并合并评分建议；示范回答只复用用户原回答中的已有内容，所有背景、职责、方案取舍、指标和结果等未提供事实均使用明确方括号占位，信息不足时提示先补充真实事实，不自由生成公司、项目、技术栈或量化结果。
- 新增 `GET /api/reports/{report_id}/question-reviews` 和 `GET /api/interviews/{session_id}/question-reviews`；两个接口返回同一批持久化快照，按题号和评分创建顺序稳定排序，并校验报告/会话属于当前用户，跨用户访问返回 404。
- 测试覆盖每条评分一一生成、必填字段完整性、报告/会话双读取、重复读取幂等、来源评分变化后快照稳定、损坏复盘自动修复、不编造事实占位模板和权限隔离；`python -m compileall -q app test` 通过，`python -m pytest test -q` 全量 156 项通过。

### BE-B2C-007 专项练习与再测

优先级：P1

状态：已完成（2026-08-23）

任务：

- 新增练习来源模型。
- 从报告短板或题目复盘创建专项练习。
- 复用面试会话处理流程创建 `weakness_practice` 会话。
- 专项完成后创建 `retest` 会话。
- 生成前后对比结果。

验收：

- 报告短板可以一键创建专项练习。
- 专项练习不会覆盖原报告。
- 再测结果可以与原评分对齐。
- 对比结果可以重复读取，不因后续训练漂移。

完成记录：

- 新增独立 `PracticeSession` 练习来源模型，保存所属用户、来源报告/会话/题目复盘/评分、短板 key 与标题、目标维度、练习类型、专项及再测会话、状态、前后分数、对比归组和来源/对比 JSON 快照；专项练习和再测继续复用 `InterviewSession`，生产库由现有启动期建表机制创建新表，并以增量列迁移为历史 `interview_sessions` 补充 `practice_context_json`。
- 新增 `POST /api/practice/from-report`、`POST /api/practice/from-question-review`、`GET /api/practice`、`POST /api/practice/{id}/start`、`POST /api/practice/{id}/start-retest` 和 `GET /api/practice/{id}/comparison`；报告入口会校验短板确实属于来源报告，逐题入口会校验报告、复盘、评分、题号和短板 key 一致，所有读写均按当前用户隔离，伪造或跨用户来源不能创建、读取或启动练习。
- 创建练习时即冻结目标岗位、原题、原回答、原始总分/子分、扣分原因、短板、建议结构、简历/JD 会话快照和报告短板顺序，不修改来源报告；报告短板入口会匹配对应题目复盘，无直接匹配时选择最低分复盘作为可追溯练习来源，保证专项上下文包含原题与原评分。
- 专项会话使用 `session_purpose=weakness_practice`、训练模式和 4 题计划，再测会话使用 `session_purpose=retest`、实战模式和 2 题计划；练习会话启动/结束和再测结束会在面试事务内将状态依次推进为未开始、训练中、待再测、已完成，再测重复启动返回同一会话，避免重复创建。
- Agent 状态、规划、后续出题和评分提示已注入不可信练习来源快照；同类题与报告短板练习不复述原题，`repeat_question` 仅允许第一题重练原题，其余问题继续围绕同一能力点但更换题面；再测不泄露来源回答、扣分原因或改进提示。
- 再测结束时按持久化评分生成一次前后对比快照，展示总分及子分变化、原短板未再次出现项、当前剩余短板、两版回答、回答结构和下一短板；后续修改来源评分、再测评分或报告内容不会重算已保存对比。前端补齐专项结束自动启动再测、再测结束自动进入对比页，以及待再测页手动恢复入口。
- 测试覆盖报告短板/逐题复盘两类创建、来源字段防伪、报告不变性、状态推进、再测幂等、总分/子分/短板/回答结构对齐、快照抗后续数据漂移和权限隔离；`python -m compileall -q app test` 通过，`python -m pytest test -q` 全量 160 项通过，`npm run build` 通过。

### BE-B2C-008 报告生命周期修正

优先级：P1

状态：已完成（2026-08-23）

任务：

- 仅结束后的完整面试生成最终报告。
- 未结束会话只允许生成预览，不缓存为最终报告。
- 新增评分后使旧草稿或预览失效。
- 报告保存 `generated_from_score_count` 或等价快照信息。

验收：

- 进行中的面试不能生成误导性的最终报告。
- 面试结束报告覆盖全部评分。
- 历史最终报告可复现。

完成记录：

- `InterviewReport` 新增 `is_final` 和 `generated_from_score_count`；生产库启动期增量迁移会结合会话最终态、用途、报告后是否还有新评分及已有逐题复盘数量识别历史最终报告和旧预览，并按已保存评分数量回填生成快照计数；识别为旧预览的记录会在面试结束后首次读取时被完整最终快照替换。
- 进行中的完整面试及专项练习/再测会话每次按当前评分生成 `is_final=false`、无持久化 ID 的临时预览；预览不写入 `interview_reports`、不进入历史列表，新增评分后再次请求会基于新评分数重新生成。
- 仅 `status=finished` 且 `session_purpose=full_interview` 的会话会持久化最终报告、全部评分计数和逐题复盘；重复读取直接返回已保存快照，不因后续评分或知识源变化重算历史最终报告。
- 报告历史、报告详情、持久化逐题复盘、练习来源和新会话来源报告均只接受最终报告；前端会明确标注临时预览及当前评分数，预览中隐藏专项练习操作并以当前评分作为逐题展示兜底。
- 测试覆盖一条评分预览、新增评分后两条评分预览、预览不落库/不进历史/不生成持久化复盘、结束后生成最终报告、评分数快照、历史重复读取及旧数据迁移分类；`python -m compileall -q app test` 通过，`python -m pytest test -q` 全量 161 项通过，`npm run build` 通过。

### BE-B2C-009 埋点与指标

优先级：P2

状态：已完成（2026-08-23）

任务：

- 新增事件记录表或轻量日志事件。
- 记录首次体验漏斗和闭环行为。
- 指标先进入后端日志或数据库，不急于接第三方分析平台。

建议事件：

- `resume_uploaded`
- `resume_pasted`
- `jd_pasted`
- `profile_auto_generated`
- `profile_applied`
- `interview_created`
- `interview_started`
- `interview_finished`
- `report_viewed`
- `question_review_expanded`
- `practice_created`
- `practice_finished`
- `retest_finished`
- `comparison_viewed`

验收：

- 能统计注册到首次面试启动转化率。
- 能统计报告到专项练习点击率。
- 能统计 7 天内复练率。

完成记录：

- 新增第一方 `analytics_events` 事件表，保存用户、事件名、会话/报告/练习/逐题复盘/简历/JD 关联 ID、受控属性、发生时间和幂等键，并为事件时间、用户时间、报告及练习查询建立索引；新表继续由启动期 `Base.metadata.create_all` 创建，暂不接第三方分析平台。
- 14 个建议事件已全部接入：简历上传/粘贴、JD 粘贴、自动画像生成、画像应用、面试创建/启动/完成、最终报告查看、逐题复盘展开、专项练习创建/完成、再测完成和对比查看。关键业务事件由后端在原事务内自动记录并使用保存点隔离埋点故障，重试通过幂等键去重；只有后端无法判断的画像应用和复盘展开由前端调用受限事件接口上报。
- 新增 `POST /api/analytics/events`，仅允许登录用户上报 `profile_applied` 和 `question_review_expanded`，校验简历、JD、报告及逐题复盘归属，拒绝客户端伪造面试启动、完成等服务端事件；前端在自动画像确认和逐题复盘真实展开时完成接入，上报失败不阻断主流程。
- 新增管理员专用 `GET /api/analytics/metrics`，默认统计最近 30 个自然日并支持 `period_start`、`period_end`；返回首次体验各阶段去重用户数、注册到首次完整面试启动转化率、最终报告首次查看到专项练习创建的去重报告点击率、事件计数及指标口径说明。
- 7 天复练率以用户首次完成完整面试为起点，只纳入已满 7 天观察窗的用户；7 天内再次启动完整面试或专项练习计为复练，闭环内再测不冒充留存。首次完成时间在数据库聚合，候选启动事件限制在对应观察窗内，避免历史事件量线性加载和近期用户被提前判定未复练。
- 测试覆盖客户端事件白名单、资源归属隔离、同一请求幂等、服务端事件防伪、首次体验转化、报告到练习点击、成熟 7 天复练观察窗及近期用户排除；`python -m compileall -q app test` 通过，`python -m pytest test -q` 全量 163 项通过，`npm run typecheck` 和 `npm run build` 通过。

## 12. 测试计划

### 12.1 后端测试

必须新增：

- 简历粘贴解析接口测试。
- 简历上传解析接口测试。
- JD 解析接口测试。
- 自动画像生成测试。
- 面试创建兼容旧字段测试。
- 不同面试类型的计划生成测试。
- 实战模式隐藏实时评分的 API 数据测试。
- 逐题复盘生成测试。
- 报告短板创建专项练习测试。
- 再测对比测试。
- 权限隔离测试：用户 A 不能访问用户 B 的简历、JD、报告和练习。
- Prompt injection 测试：简历和 JD 中的恶意指令不能改变系统角色和评分规则。

建议命令：

```powershell
cd backend
python -m pytest test -q
```

注意：完整后端测试依赖 MySQL、Qdrant、RabbitMQ 等服务。只做文档和前端构建时，不需要启动这些服务；做接口和 RAG 联调时必须启动。

### 12.2 前端测试

必须新增或手工验证：

- 新用户首次体验三步流程。
- 粘贴简历、上传简历、粘贴 JD 的错误状态。
- 自动画像确认和手动编辑。
- 训练/实战模式切换。
- HR、项目深挖、技术基础、系统设计类型选择。
- 报告逐题复盘展开收起。
- 短板一键专项练习。
- 再测后前后对比页。
- 移动端 390x844、桌面端 1440x900 布局。
- 按 `前端移动端适配任务跟踪.md` 的 FE-MOB-101～FE-MOB-112 标准做响应式回归，确保新增 B2C 页面不破坏已有移动端适配。
- 涉及面试聊天、报告详情、上传表单、粘贴 JD、专项练习和前后对比时，额外验证 320×568、844×390 横屏、软键盘聚焦缩放和长文本换行。

建议命令：

```powershell
cd frontend
npm run build
```

### 12.3 质量底线

- 不允许新增 TypeScript 类型错误。
- 不允许报告复盘空字段大面积展示。
- 不允许旧会话打不开。
- 不允许实战模式提前暴露评分。
- 不允许简历/JD 中的用户文本作为系统指令执行。
- 不允许专项练习修改原报告。
- 不允许破坏 `前端移动端适配任务跟踪.md` 已完成的底部导航、安全区、软键盘、横向溢出、无障碍、截图基线和按需构建约束。

## 13. 分阶段排期

### M1：定位统一与首次体验入口

目标：让产品看起来就是 B2C 技术求职者工具，并能用简历/JD 快速开始训练。

范围：

- 品牌文案统一。
- 简历粘贴解析。
- JD 粘贴解析。
- 自动画像草稿。
- 开始训练页三分钟入口。
- 面试创建支持模式和类型字段。

不包含：

- 完整专项练习闭环。
- 前后对比。
- 复杂简历版本管理。
- 语音。

完成标准：

- 新用户能在一个页面完成简历/JD/画像/开练。
- 前端构建通过。
- 后端关键接口测试通过。

### M2：逐题复盘报告

目标：让报告从总结页变成可执行的训练清单。

范围：

- 逐题复盘数据生成。
- 报告详情页展示问题、原回答、扣分原因、建议结构、示范回答。
- 每题练习入口。
- 报告短板排序。

完成标准：

- 每道评分题都有复盘卡片。
- 复盘内容可追溯到评分记录。
- 用户知道下一步该练什么。

### M3：专项练习与再测

目标：打通核心留存闭环。

范围：

- 从短板创建专项练习。
- 从题目复盘创建同类题练习。
- 专项练习结束后进入再测。
- 前后对比报告。

完成标准：

- 用户从报告出发可以完成一次“练短板 -> 再测 -> 看提升”。
- 对比结果稳定保存。
- 复练率可以统计。

### M4：体验和数据优化

目标：根据真实使用数据优化转化和留存。

范围：

- 首次体验漏斗埋点。
- 报告点击和练习转化分析。
- 页面细节、空状态、移动端体验优化。
- 按岗位补充知识库和评测集。

完成标准：

- 能回答多少用户上传简历、多少用户看报告、多少用户进入专项练习、多少用户完成再测。
- 能根据数据确定下一轮重点，而不是凭感觉优化。

## 14. 任务优先级总表

| 编号 | 优先级 | 模块 | 任务 | 依赖 | 验收摘要 |
| --- | --- | --- | --- | --- | --- |
| FE-B2C-001 | P0 | 前端 | 品牌文案统一 | 无 | B2B 文案清理 |
| BE-B2C-001 | P0 | 后端 | 简历模型与解析 | 无 | 粘贴/上传简历可解析 |
| BE-B2C-002 | P0 | 后端 | JD 模型与解析 | 无 | JD 可生成岗位画像 |
| BE-B2C-003 | P0 | 后端 | 自动画像生成 | 简历/JD | 生成草稿不直接覆盖 |
| BE-B2C-004 | P0 | 后端 | 面试会话字段扩展 | 简历/JD | 兼容旧会话 |
| FE-B2C-002 | P0 | 前端 | 三分钟入口 | 简历/JD/画像接口 | 一页完成首次开练 |
| FE-B2C-003 | P0 | 前端 | 模式与类型选择 | 会话字段 | 实战隐藏评分 |
| BE-B2C-005 | P0 | Agent | 按类型出题 | 会话字段 | 类型与题目匹配 |
| BE-B2C-006 | P0 | 后端 | 逐题复盘 | 评分记录 | 每题复盘完整 |
| FE-B2C-004 | P0 | 前端 | 报告逐题复盘 | 复盘接口 | 可查看并发起练习 |
| BE-B2C-007 | P1 | 后端 | 专项练习与再测 | 复盘 | 可生成前后对比 |
| FE-B2C-005 | P1 | 前端 | 对比视图 | 练习接口 | 再测后展示提升 |
| BE-B2C-008 | P1 | 后端 | 报告生命周期修正 | 报告服务 | 不缓存未完成最终报告 |
| BE-B2C-009 | P2 | 数据 | 埋点与指标 | 核心流程 | 可看漏斗和复练率 |

## 15. MVP 验收清单

M1+M2 完成后，可以邀请第一批真实技术求职者内测。验收清单如下：

- 用户能明确看出产品是给个人技术求职者使用的。
- 用户能上传或粘贴简历。
- 用户能粘贴 JD。
- 系统能生成求职画像草稿。
- 用户能在三分钟内开始第一场训练。
- 用户能选择训练模式或实战模式。
- 用户能选择 HR、项目深挖、技术基础、系统设计或综合面试。
- 训练模式可以看到即时反馈。
- 实战模式过程中不暴露评分。
- 报告按题展示问题和原回答。
- 报告按题解释扣分原因。
- 报告按题给出建议结构。
- 报告按题给出不编造事实的示范回答。
- 报告展示最关键的短板排序。
- 用户可以从报告进入专项练习入口。

M3 完成后，才算真正形成留存闭环：

- 用户能从报告短板一键开始专项练习。
- 用户能完成再测。
- 用户能看到前后对比。
- 系统能推荐下一项短板。
- 后台能统计报告到专项练习、专项练习到再测的转化率。

## 16. 风险与取舍

### 16.1 简历解析风险

风险：

- PDF 解析不稳定。
- 简历格式差异大。
- 模型可能抽取错误。

取舍：

- 第一阶段优先支持粘贴文本，上传文件作为增强。
- 自动画像必须让用户确认，不自动覆盖。
- 保留原文和解析结果，方便回溯。

### 16.2 JD 解析风险

风险：

- JD 可能非常长，包含公司套话。
- JD 可能混入恶意 prompt。

取舍：

- 限制最大长度。
- 将 JD 明确标记为不可信输入。
- 解析结果只作为岗位参考，不覆盖系统评分规则。

### 16.3 报告内容质量风险

风险：

- 模型生成示范回答可能编造经历。
- 逐题复盘太长，用户读不完。

取舍：

- 示范回答必须基于原回答和简历事实。
- 每题复盘默认折叠，先展示扣分原因和建议结构。
- 示范回答允许使用“你可以补充真实指标”这样的占位提示。

### 16.4 专项练习复杂度风险

风险：

- 如果一开始做成完整课程系统，会拉长周期。

取舍：

- 第一版专项练习只复用现有面试会话。
- 用 `session_purpose` 区分全量面试、专项练习、再测。
- 先证明“短板 -> 练习 -> 再测 -> 对比”闭环成立，再做课程化。

## 17. 最终建议

下一步不要优先做语音，也不要急着做企业功能。当前最值得推进的是：

1. 先统一 B2C 技术求职者定位。
2. 再把简历、JD、画像自动生成接到开始训练页。
3. 然后把报告改成逐题复盘。
4. 最后打通专项练习和再测对比。

这条路径最贴合当前项目已有架构，能最大化复用现有 LangGraph、评分、报告和 RAG 能力，也最容易让真实用户感到产品有用。
